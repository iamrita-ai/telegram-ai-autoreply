# ── Telegram ─────────────────────────────────────────────────────────────────
telethon>=1.38,<2

# ── HTTP / web ───────────────────────────────────────────────────────────────
httpx>=0.27,<1
aiohttp>=3.10,<4                # health endpoint (Render needs a bound port)

# ── Storage ──────────────────────────────────────────────────────────────────
motor>=3.6,<4                   # async MongoDB driver
pymongo>=4.9,<5
cryptography>=43.0              # Fernet encryption for session strings

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv>=1.0,<2
# python:*-slim ships NO IANA timezone database, so zoneinfo cannot resolve
# "Asia/Kolkata" (or even "UTC") and every schedule silently ran on UTC.
tzdata>=2024.2
phonenumbers>=8.13             # normalises phone numbers at login
